import "./style.css";
import { PerspectiveCamera, HemisphereLight, DirectionalLight } from "three";
import { app } from "./main";

// var hemisphereLight = new HemisphereLight(0xaaaaa, 0x000000, 0.9);
// var shadowLight = new DirectionalLight(0xffffff, 0.9);
// shadowLight.position.set(150, 350, 350);

// shadowLight.castShadow = true;

// shadowLight.shadow.camera.left = -400;
// shadowLight.shadow.camera.right = 400;
// shadowLight.shadow.camera.top = 400;
// shadowLight.shadow.camera.bottom = -400;
// shadowLight.shadow.camera.near = 1;
// shadowLight.shadow.camera.far = 1000;

// shadowLight.shadow.mapSize.width = 2048;
// shadowLight.shadow.mapSize.height = 2048;

// app.renderer.scene.add(hemisphereLight);
// app.renderer.scene.add(shadowLight);
